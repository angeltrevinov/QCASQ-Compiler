program Example;
class Persona: hola {
    var edad[9][3]: int;
    var nombre: string;

    func UNO(a: int): int {
        return (edad - x);
    }
    constructor() {
        var edad: int;
        edad = (edad - x);

        while(i < j) {
            i = i+j;
        }
    }
};

var i, j, p: int;
var estudiante: Persona;

func fact(b: int, m: int): int {
    var i: int;
    i = j + (p - j*2+j);
    if (j == 1) {
        return j;
    } else {
        return j * fact(j-1);
    }
}

func pelos(l: entero) {
    var x: int;
    input(estudiante);
    x = y;
}

main() {
    var all: int;
    input(p);
    //j = p*2;
    i = fact(p);
    input(estudiante);
    pelos(p);
}