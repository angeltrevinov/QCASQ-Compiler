program Example;
class Persona {
    var edad: int;
    var nombre: string;
    //output(estudiante.edad);
    func UNO(x: int): int {
    //return (edad - x);
    }
    constructor() {
    }
};
var i, j, p: int;
var estudiante: Persona;
func fact(j: int): int {
    var i: int;
    i = j + (p - j*2+j);
    if (j == 1) {
        return j;
    } else {
        return j * fact(j-1);
    }
}
func pelos(y: entero) {
    var x: int;
    input(estudiante);
    x = y;
}

main() {
    input(p);
    j = p*2;
    i = fact(p);
    input(estudiante);
    pelos(p);
}