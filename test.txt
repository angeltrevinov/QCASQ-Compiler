program Example;
class Persona {
    var edad[9][3]: int;
    var nombre: string;

    func UNO(x: int, m: float): int {
        return (edad - x);
    }
    constructor() {
        var edad: int;
        edad = (edad - x);

        while(i < j) {
            i = i+j;
        }
    }
};

var i, j, p: int;
var estudiante: Persona;

func fact(j: int): int {
    var i: int;
    i = j + (p - j*2+j);
    if (j == 1) {
        return j;
    } else {
        return j * fact(j-1);
    }
}

func pelos(y: entero) {
    var x: int;
    input(estudiante);
    x = y;
}

main() {
    var all: int;
    input(p);
    //j = p*2;
    i = fact(p);
    input(estudiante);
    pelos(p);
}