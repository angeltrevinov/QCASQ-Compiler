//testCases/testQuadruples.txt
program Example;

var x: int;
var j: float;

func adios(){
    var j: int;
    var f: float;
}

func hola(){
    var j: int;
    var f: float;
    x = j + x / 4 - (j * 3 + x);
}

main() {
    x = 3;
    var one: int;
    var two: int;
    two = two * one + 3;
}