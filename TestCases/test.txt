program Example;
class Persona{
    var edad[9][3]: int;
    var nombre: string;

    func UNO(x: int, m: float): int {
        return (edad - x);
    }
    constructor(x: int, i:int, j:int) {
        var edad: int;
        edad = (edad - x);

        while(i < j) {
            i = i + "hola";
        }
    }
};

var i, j, p: int;
var estudiante: Persona;

func fact(j: int): int {
    var i: int;
    i = j + (p - j*2+j);
    if (j == 1) {
        return j;
    } else {
        return j * fact(j-1);
    }
}

func pelos(y: entero) {
    var x: int;
    input(estudiante);
    x = y + false;
}

main() {
    var all: int;
    input(p);
    // This is a comment
    i = fact(p);
    input(estudiante);
    output(p, all);
}