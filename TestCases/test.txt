program Example;
class Persona{
    var edad[9][3]: int;
    var nombre: string;

    func UNO(x: int, m: float): int {
        //return (edad - x);
    }
    constructor(x: int, i:int, j:int) {
        var edad: int;
        edad = (edad - x);

        while(i < j) {
            //i = i + "hola";
            i = i + 69;
        }
    }
};

var i, j, p: int;
var estudiante: Persona;

func fact(j: int): int {
    var i: int;
    i = j + (p - j*2+j);
    if (j == 1) {
        // return j;
        j = j + i;
    } else {
        // return j * fact(j-1);
        j = j * j / 1;
    }
}

func pelos(y: entero) {
    var x: int;
    // input(estudiante);
    x = 3 * 8 + i;
    //x = y + false;
}

main() {
    var all: int;
    //input(p);
    // This is a comment
    all = all * 3 / 8 * all;
    //i = fact(p);
    //input(estudiante);
    //output(p, all);
}